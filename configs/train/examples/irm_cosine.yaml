# @package _global_

defaults:
  - _self_
  - override /train: phase2

irm:
  enabled: true
  type: cosine
  lambda: 0.1
  normalize: l2
  eps: 1.0e-12
  env_min: 2
  freeze_backbone: true
  logging:
    log_irm_grads: true
