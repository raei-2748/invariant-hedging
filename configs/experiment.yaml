# Master Hydra experiment configuration
# Provides defaults for training and evaluation workflows.
defaults:
  - _self_
  - /data: synthetic
  - /envs: daily
  - /costs: low
  - /model: erm
  - /eval: daily
  - /logging: default
  - /train: erm

runtime:
  seed: 0
  device: auto
  mixed_precision: true
  output_dir: runs
