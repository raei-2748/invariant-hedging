defaults:
  - model: hedger
  - data: regimes
  - algorithm: irm
  - training: default
  - evaluation: default
  - logging: wandb
  - _self_

project:
  name: hirm_experiment
  seed: 0
  output_dir: outputs/reproduce

hydra:
  run:
    dir: ${project.output_dir}
  output_subdir: null

wandb:
  mode: disabled
